version: 3
projects:

- dir: .
  workflow: custom1
  terraform_version: v1.1.4 
  autoplan:
    when_modified: [ "*.tf*" ]
workflows:
  custom1:
    plan:
      steps:
      - init

      - run: echo "Executando terraform plan terraform"
      - plan
    apply:
      steps:
      - run: echo "Executando terraform apply terraform"
      - apply  
    